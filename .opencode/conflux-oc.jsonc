{
  // ─── conflux-oc plugin configuration ───
  //
  // This file is optional. All settings have safe defaults.
  // User data (habits, conventions) is stored in:
  //   ~/.config/opencode/plugins/conflux-oc/users/<USER>.json
  //
  // No configuration keys are currently consumed by the plugin.
  // This file serves as a placeholder for future options.
}
